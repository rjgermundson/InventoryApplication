<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.riley.inventoryapplication">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".View.Hub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.AddEntry"
            android:screenOrientation="portrait" />
        <activity android:name=".Camera.Camera"
            android:screenOrientation="portrait" />
        <activity android:name=".View.ProductEntry"
            android:screenOrientation="portrait" />
        <activity android:name=".View.SearchScreen"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".View.ImportPage"
            android:screenOrientation="portrait" />
        <activity android:name=".View.Settings"
            android:screenOrientation="portrait" />
    </application>

</manifest>